# pdfcontract
A web program that make contracts in pdf format conveniently

终端在激活虚拟环境时，使用命令：

pip  freeze  >requirements.txt

安装一个虚拟环境的依赖：

pip  install   -r requirements.txt


本地:

export FLASK_APP=flaskr
export FLASK_ENV=development
